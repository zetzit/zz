using warloc;
using <assert.h>::{assert};

pub fn main() -> int{
    char mut buf[32];
    warloc::into_warloc_pool(&buf, sizeof(buf));

    assert(warloc::malloc(16) != 0);
    assert(warloc::malloc(16) == 0);

    return 0;
}
