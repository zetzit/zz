using string;
using <stdio.h>::{getline, printf, stdin};
using <stdlib.h>::{free};


export fn main() -> int {
    string::String+50 mut s = {0};
    string::clear(&s);

    for (uint mut i =0; i < 100; i++) {
        string::push(&s, ' ');
    }

    return 0;
}
