export import c::stdint;
export import c::stdio;
import math::add;

import constants::foo;
import errors::error;

fn some_helper(mutable error* err, mutable uint32_t* bob) -> uint32_t {
    printf("lol\n");
    if (bob) {
        *bob = add(horst(), foo);
        printf("bob %d\n", *bob);
    }
    return 32;
}

export fn horst() -> uint32_t {
    return 3;
}
