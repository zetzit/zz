import stdio;
import stdint;
import errors::error;
import math::add;

fn some_helper(mutable error* err, uint32_t* bob) -> uint32_t {
    printf("lol\n");
    if (bob) {
        *bob = add(1, 2);
        printf("bob %d\n", *bob);
    }
    return 32;
}
