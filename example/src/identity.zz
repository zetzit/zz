import errors::error;
import math::add;


#include <stdint.h>
#include <stdio.h>

fn some_helper(mutable error* err, mutable uint32_t* bob) -> uint32_t {
    printf("lol\n");
    if (bob) {
        *bob = add(1, 2);
        printf("bob %d\n", *bob);
    }
    return 32;
}
