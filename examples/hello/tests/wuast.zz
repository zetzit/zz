using <assert.h>::{assert};
using <stdlib.h>::{exit};


struct bob{}

fn bob_is_a_failure(bob * unsafe b) {
    exit(6);
}

test yes_bob_is_a_failure {
    exit = 6;
}


export fn main() -> int {
    bob_is_a_failure(0);
    return 0;
}
