using constants;
using error::{Error};


enum Bla {
    Bob,
    Lala,
}


fn usemeeee(Error* e) {
}

using <stdint.h>::{uint8_t};
using <memory.h>::{memset};

pub fn new(Error mut* self) {
    memset(self, 0, sizeof(Error));



    int mut o = 0;
    for (int mut i = 1; i < 1; i++, o++) {
    }
}
