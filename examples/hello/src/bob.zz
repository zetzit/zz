using constants;
using err::{Err};


enum Bla {
    Bob,
    Lala,
}


fn usemeeee(Err* e) {
}

using <stdint.h>::{uint8_t};
using <memory.h>::{memset};

pub fn new(Err mut* self) {
    memset(self, 0, sizeof(Err));



    int mut o = 0;
    for (int mut i = 1; i < 1; i++, o++) {
    }
}
