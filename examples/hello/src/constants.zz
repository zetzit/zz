pub const int foo = 3 * 6;
using <math.h>::{floor};
using <memory.h>::{memset};

using aye;

export fn conflict() -> aye::bob {
    aye::bob mut r = {0};
    memset(&r, 0, sizeof(aye::bob));
    return r;
}
