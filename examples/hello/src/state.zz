using libc::stdint::{uint8_t};
using libc::string;
using libc::string::{memset};


pub struct State {
#if def("debug")
    #if 0
        uint19_t invalid;
    #elif 0
    #else
        uint8_t yalla;
    #endif
#endif
    uint8_t counter;
}

#if 0
    const int lol = 3;
#endif

pub fn new(State mut* self) {
    memset(self, 0, sizeof(State));
#if 0
    return nothing;
#endif
}

pub fn add(State mut* self, int i) {
    self->counter += i;
    goto exit;
exit:
    return;
}
