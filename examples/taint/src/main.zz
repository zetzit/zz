using <stdio.h>::{printf};



struct Socket {
    int mut fd;
}



fn open(Socket set<open> mut* self) {
    self is safe;
    self->fd = 2;
}

fn boop(Socket mut* self) {

}

fn read(Socket require<open> mut* self) {

}

export fn main() -> int {

    Socket mut sock = {0};
    open(&sock);
    read(&sock);

    printf("hello taint\n");
    return 0;
}
