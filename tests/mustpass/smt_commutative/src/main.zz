using <stdio.h>::{printf};




fn rand() -> usize {
    return 42;
}


export fn bob(int * unsafe i, usize y)
    where len(i) >= y
{
    
}


export fn main() -> int {


    int     *i = rand();
    usize   y = rand();
    static_attest(y <= len(i));

    bob(i, y);

    printf("hello smt_lesseq\n");
    return 0;
}
